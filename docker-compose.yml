version: '3.8'

services:
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    networks:
      - backend
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - auth-service
      - product-service
      - order-service
      - payment-service
      - edge-service
    networks:
      - backend
    restart: unless-stopped

  auth-service:
    build: ./auth-service
    env_file: ./auth-service/.env
    networks:
      - backend
    restart: unless-stopped

  product-service:
    build: ./product-service
    env_file: ./product-service/.env
    ports:
      - "8002:3002"
    networks:
      - backend
    restart: unless-stopped

  order-service:
    build: ./order-service
    env_file: ./order-service/.env
    depends_on:
      - product-service
      - payment-service
    networks:
      - backend
    restart: unless-stopped

  payment-service:
    build: ./payment-service
    env_file: ./payment-service/.env
    networks:
      - backend
    restart: unless-stopped

  edge-service:
    build: ./edge-service
    env_file: ./edge-service/.env
    ports:
      - "8005:3005"
    networks:
      - backend
    restart: unless-stopped

networks:
  backend:
    driver: bridge

